---
title: 'Installation'
---

Library is available on npm. You can install it using the following command:

```bash
npm install suvidha
```

Make sure to install the peer dependency `zod` and `express` as well if you haven't already.

```bash
npm install zod express
```
